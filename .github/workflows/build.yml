name: Build Windows EXE

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      # 1️⃣ Puxar o código do repositório
      - uses: actions/checkout@v4

      # 2️⃣ Configurar Python
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      # 3️⃣ Instalar dependências e Playwright
      - name: Instalar dependências
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          playwright install --with-deps

      # 4️⃣ Copiar Chromium completo do cache do Playwright
      - name: Preparar Chromium para PyInstaller
        run: |
          echo "🔹 Criando pasta do Chromium..."
          mkdir -p automacoes\playwright_browsers\chrome-win32
          echo "🔹 Copiando arquivos do Chromium..."
          xcopy /E /I /Y "%LOCALAPPDATA%\ms-playwright\chromium-1208\chrome-win32" automacoes\playwright_browsers\chrome-win32\
          echo "✅ Chromium preparado com sucesso."

      # 5️⃣ Gerar EXE com PyInstaller
      - name: Gerar EXE
        run: |
          echo "🔹 Rodando PyInstaller..."
          pyinstaller --onefile --log-level=DEBUG automacoes/main.py
          echo "✅ Build do EXE concluído."

      # 6️⃣ Upload do EXE gerado como artefato
      - name: Upload artefato
        uses: actions/upload-artifact@v4
        with:
          name: automacao-windows
          path: dist/*.exe
          retention-days: 7